
## 2026.08.03 (월)
## 파이썬 함수, 위치 인수, 키워드 인수, 재귀호출, 람다 표현식, 클로저, 클래스

> 이 내용들은 Google Gemini의 도움을 받아 정리하였음을 밝힙니다

### 함수

자주 쓰는 코드들을 하나로 예쁘게 묶어놓은 상자 같은거. `def` 키워드로 시작

### 위치 인수 

함수에 값을 넘겨줄 때, **정해진 순서대로** 얌전하게 들어가는 인수

**주의 : 순서가 틀리면 엉뚱한 변수에 값이 들어가니까 순서를 꼭 맞추자**

### 키워드 인수 

`이름 = 값` 형태로 이름표를 딱 붙여서 넘겨주는 인수.

이름표를 붙여주면 순서를 맘대로 섞어도 알아서 자기 자리를 찾아가니까 위치가 헷갈릴 것 같으면 이걸 쓰자 

### 재귀호출

함수 안에서 자기 자신을 또 부르는 것. 

**주의 : 종료 조건을 안 만들어두면 무한 루프에 빠져서 프로그램이 뻗어버린다**

### 람다 표현식 

`def`를 쓰기도 아까울 만큼 짧은 **1줄짜리 익명 함수** (`lambda 매개변수 : 표현식`)

이름도 없이 잠깐 쓰고 버릴 때만 쓰는거라서 너무 복잡한 코드를 람다로 우겨넣으면 나중에 본인도 못 알아보니까 적당히 쓰자.

### 클로저 

바깥쪽 함수가 이미 끝나서 사라졌는데도, 안쪽 함수가 바깥쪽 함수의 변수를 끈질기게 기억하고 있는 녀석

데이터를 안전하게 숨겨두고 싶거나 자기만의 맞춤형 함수를 찍어낼 때 쓰는 고급 기술이니까 잘 기억해두자

### 클래스

객체를 펑펑 찍어내는 설계도(틀). 흔히 붕어빵 틀에 비유.

비슷한 성질을 가진 데이터(속성)랑 기능(메서드)을 한 묶음으로 예쁘게 포장해 주는 거.

----

|**수업 중에 만든 파일들**|
|:--|
|[11_fn.py](../workspace_python/11_fn.py)|
|[12_class.py](../workspace_python/12_class.py)|


