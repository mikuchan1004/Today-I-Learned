## 2026.07.08(수)
### 반복문 - for문

프로그래밍에서 동일하거나 유사한 동작이 반복해서 나올 때는 반복문을 사용.

반복문 역시 조건문처럼 프로그래밍의 가장 기초적인 문법 중 하나.

- for문의 기본 사용법

``` javascript
for (초기식; 조건식; 증감식) {
  실행할 명령
}
```

- for문은 조건식과 일치하지 않을 때까지 반복하면서 중괄호에 있는 명령을 수행.

- break 문을 사용하면 조건식에서 정의한 반복문의 종료 조건과 상관없이 반복을 강제로 종료할 수 있다.

``` javascript
for (let idx = 1; idx <= 100; idx++){
  if (idx > 10) {
    console.log('반복문 출력 종료!');
    break;
  }
  console.log(idx);
}
```

- continue 문은 break 문과 유사하게 동작하지만, 반복을 멈추는 대신 다음 반복 과정으로 건너뛴다.

``` javascript
for (let idx = 1; idx <= 10; idx++) {
  if(idx % 2 === 0) {
    continue;
  }
  console.log(idx);
}
```

-------------------------------------------------------
| 수업 중에 만든 파일들 |
| :--- |
[04_for.html](../workspace_html/javascript/04_for.html)

